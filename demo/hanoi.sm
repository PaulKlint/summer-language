proc hanoi(n, ns, nd, ni)
(
  if n = 0 then
     return
  else
     hanoi(n-1, ns, ni, nd);
     put('move disc ', n, ' from ', ns, ' to ', nd, '\n');
     hanoi(n-1, ni, nd, ns)
  fi
);

program main()
  hanoi(5, 'a', 'b', 'c')
