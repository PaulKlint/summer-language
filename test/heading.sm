# common procedures for testset #

proc error(n) put('ERROR: ', n, '\n');

proc test(n) put('TEST ', n, '\n');

proc start(name) put('START ', name, '\n');

proc finish(name) put('FINISH ', name, '\n');

