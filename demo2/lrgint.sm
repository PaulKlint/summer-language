class repr(seg, link)
begin	fetch seg, link;
	store seg, link;
end repr;
class lrgint(s)
begin fetch +, -, *, /, print;

	var r;
	proc add(l2, c)
	( var n, l1;
	  l1 := copy(self);
	  l1.seg := seg + l2.seg + c;

init:
	if type(s) = 'string' then
	   if s.size <= width then
	      seg := integer(s);
	   else
	      scan s
	      for
		  link := lrgint(rtab(width));
		  seg  := integer(rtab(0));
	      rof
	   fi
	elif type(s) = 'integer' then
	   seg := s
	else
	   stop(-1)
end class lrgint;
